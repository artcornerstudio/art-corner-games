# Copy to .env.local (gitignored by Vite) or set in your CI build step.

# Base URL of the deployed "Ask Coach" Cloudflare Worker (see coach/README.md),
# with no trailing slash. Optional: when it is unset the app still works and
# the Ask Coach card answers from built-in templates ("Offline coach").
VITE_COACH_URL=
