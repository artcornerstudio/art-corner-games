{
  "name": "football-iq-coach",
  "private": true,
  "version": "0.1.0",
  "description": "Cloudflare Worker that answers a kid's question about a Football IQ play through the Claude API",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.127.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260920.1",
    "typescript": "^5.6.3",
    "wrangler": "^4.135.0"
  }
}
