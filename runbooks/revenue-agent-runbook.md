# Revenue Agent Runbook

## Workflow

1. Coordinator invokes `market-signal-researcher`.
2. Coordinator invokes `offer-architect`.
3. Coordinator invokes `content-angle-strategist`.
4. Coordinator invokes `conversion-system-builder`.
5. Coordinator combines outputs into `outputs/revenue-agent-demo.md`.

## Coordinator Rules

- Keep agent jobs separate.
- Ask for evidence before finalizing.
- Every output must connect to a monetization path.
